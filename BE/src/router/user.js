const express = require('express')
const router = express.Router()
const {UserController} = require('../app/controller/UserController')
const middlewareVerify = require('../app/middleware/UserMiddleware')
const multer = require('multer')
const upload = multer()

// router.get('/checkOnlineUser', middlewareVerify, new UserController().checkOnlineUser)
router.post('/login', new UserController().index)
router.post('/create', new UserController().create)
router.post('/updateStory',upload.none(), new UserController().updateStory)
router.get('/profileByUserId/:userId', new UserController().profileByUserId)
router.get('/profile', middlewareVerify, new UserController().profile)
router.get('/statusOnline/:userId', new UserController().getStatusOnline)
router.get('/searchUsers', new UserController().searchUsers)
router.get('/:userId',  new UserController().getName)
module.exports = router